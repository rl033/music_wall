<h1>List of Songs</h1>
<hr/>

<% @tracks.each do |track| %>
  <div class="track">
    <h3><%= track.title %> <small>(by: <%= track.author %>)</small></h3>
    <p>
      <%= track.created_at %>
      <%= track.updated_at %>
    </p>
  </div>
<% end %>

<hr/>
<a href="/tracks/new">Add a new song</a>